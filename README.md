# Converter Program

This repository contains a simple Python utility that can parse XML, SOAP, or JSON files and output their contents as a dictionary in JSON format.

## Usage

```
python converter.py <path-to-input-file>
```

The script detects whether the input is JSON or XML (including SOAP). It then prints a pretty-printed JSON representation of the parsed data.
